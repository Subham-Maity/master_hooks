import UseState from "../components/UseState";

-

<div className="bg-gray-900">
<div className=" ml-72 mr-72 min-h-screen bg-stone-800 text-white items-center text-center">







<h1 className="font-extrabold text-5xl mb-8">useState hook</h1>

<details className="text-lg mb-4">
<summary className="font-bold text-cyan-500">What is the useState hook?</summary>
<h1 className="text-yellow-200">
he `useState` hook is a built-in hook in React that allows you to add state to a functional component. It takes an initial value as an argument and returns an array with two elements: the current state value and a function to update the state value. In this example, we use the `useState` hook to manage a simple counter. The `count` variable holds the current count, while the `setCount` function can be used to update the count.
</h1>
</details>

<UseState/>





























</div>
</div>
